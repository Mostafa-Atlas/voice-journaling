# What does this PR do?

## Checklist

- [ ] Tests added/updated, `uv run python -m unittest discover -s tests -v` green
- [ ] `uv run python scripts/scan_secrets.py` green
- [ ] No secrets, audio, transcripts, or `.db` files included
- [ ] `.env.example` / README updated if config changed
